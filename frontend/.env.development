# =============================================================
#  .env.development — Variáveis de ambiente para desenvolvimento
#
#  · URL do backend local
#  · Usado automaticamente pelo Vite em modo dev
# =============================================================

# URL do backend em desenvolvimento (localhost)
VITE_API_URL=http://localhost:3333
